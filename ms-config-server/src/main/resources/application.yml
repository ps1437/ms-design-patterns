server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ps1437/ms-design-patterns.git
          search-paths: banking-microservices/config-repo
